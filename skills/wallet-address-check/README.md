# 链上地址格式校验器 / Wallet Address Check

> `wallet-address-check` v1.0.0 · LGD Moat Family · Zero-dependency · Domain: 区块链 · Law-map: TH-LGD-019

**中文**：转账手滑地址粘错链/抄错位，资产直接归零——先过一道格式门 一条命令完成初筛：合规 PASS（rc=0）/ 违规 FAIL（rc=1）/ 用法错误（rc=2），支持 `--json`。

**EN**: One-command pre-screening for 区块链. PASS (rc=0) / FAIL (rc=1) / usage error (rc=2). `--json` supported.

## Usage / 用法
```bash
python scripts/wallet_address_check.py --help
```

## Disclaimer / 免责
Screening aid only, not legal/investment/medical advice. / 合规初筛辅助，不构成专业意见。
